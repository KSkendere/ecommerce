import { ProductDtoForAdmin } from './product-dto-for-admin';

describe('ProductDtoForAdmin', () => {
  it('should create an instance', () => {
    expect(new ProductDtoForAdmin()).toBeTruthy();
  });
});
